services:
  - type: web
    name: qgene
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn qgene_app:app
    envVars:
      - key: PYTHON_VERSION
        value: 3.12.7